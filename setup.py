from setuptools import setup

setup(
   name='Data from json show',
   version='1.0',
   description='Returns all the values found from given key',
   author='Krasniukk',
   author_email='tetiana.krasniuk@ucu.edu.ua',
   packages=['Skyscrapers'],  #same as name
   install_requires=''
)